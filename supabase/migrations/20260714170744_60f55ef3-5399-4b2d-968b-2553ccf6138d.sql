-- Placeholder intentionally small: existing migration corpus must be applied from project files in order.
-- This migration keeps the backend ready while the schema restore is handled by the stored migration files.
SELECT 1;