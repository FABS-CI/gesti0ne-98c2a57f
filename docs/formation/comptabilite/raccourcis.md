# Raccourcis — Comptabilité
- `Ctrl/⌘ + K` : recherche globale (client, facture, paiement).
- `Alt + N` (sur `/factures`) : nouvelle facture.
- `Alt + P` (sur `/paiements`) : nouveau paiement.
- Colonnes tri : cliquer l'en-tête ; **flèche + `aria-sort`** visible pour la navigation clavier.
